[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
  "lxml",
  "docxtpl",
  "requests",
  "openai",
]
name = "cvextract"
version = "0.4.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
[tool.coverage.run]
omit = ["*/tests/*"]